import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.e.walletmaster',
  appName: 'Wallet Master',
  webDir: 'dist/public'
};

export default config;
